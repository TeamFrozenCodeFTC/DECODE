package org.firstinspires.ftc.blackice.core.hardware.drivetrain;

import com.qualcomm.robotcore.hardware.HardwareMap;

public interface DrivetrainConfig {
    Drivetrain build(HardwareMap hardwareMap);
}
